apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: it-backend
  namespace: it-dev
spec:
  secretName: it-backend-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: it-backend.{{DNS_ZONE}}
  acme:
    config:
    - http01:
        ingressClass: nginx
      domains:
      - it-backend.{{DNS_ZONE}}
