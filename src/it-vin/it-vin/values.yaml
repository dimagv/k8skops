# Default values for it-vin.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: 784590408214.dkr.ecr.eu-central-1.amazonaws.com/insurancetruck/vin
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  path: /
  hosts:
    - chart-example.local
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #  cpu: 100m
  #  memory: 128Mi
  # requests:
  #  cpu: 100m
  #  memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}

mysqlHost: host
mysqlRootPassword: pass
mysqlUser: user
mysqlPassword: pass
mysqlDatabase: db

smtpHost: email-smtp.us-east-1.amazonaws.com
smtpPort: 587
smtpFrom: vin_service@insurancetruck.com
smtpTo: artem.z@ironjab.com
smtpName: name
smtpPassword: pass

vinLink: https://vintelligence3.polk.com/vindecoder/VinDecoderService
vinName: name
vinPass: pass

mainSite: it-backend
mainPort: 80
